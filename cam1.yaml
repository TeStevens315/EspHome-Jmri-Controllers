esphome:
  name: cam1
  friendly_name: CAM1

esp32:
  board: esp32dev
  framework:
    type: arduino

# Enable logging
logger:

# Enable Home Assistant API
api:
  encryption:
    key: "hNIERPFbeeGPtfDskN2BU/cDABLViqGxzKgCFABbJaM="

ota:
  - platform: esphome
    password: "ea7286667f05294ac2737aff7aa60db2"

wifi:
  # ssid: !secret wifi_ssid
  # password: !secret wifi_password
  ssid: SantaFe
  password: flatapus

  # Enable fallback hotspot (captive portal) in case wifi connection fails
  ap:
    ssid: "Cam1 Fallback Hotspot"
    password: "yk32gqhxP4Jc"

captive_portal:



esp32_camera:
  external_clock:
    pin: GPIO4
    frequency: 20MHz
  i2c_pins:
    sda: GPIO18
    scl: GPIO23
  data_pins: [GPIO34, GPIO13, GPIO14, GPIO35, GPIO39, GPIO38, GPIO37, GPIO36]
  vsync_pin: GPIO5
  href_pin: GPIO27
  pixel_clock_pin: GPIO25
  vertical_flip: true
  horizontal_mirror: true
  resolution: 800X600
  jpeg_quality: 40
  name: Cam1


# esp32_camera:
#   external_clock:
#     pin: GPIO0
#     frequency: 20MHz
#   i2c_pins:
#     sda: GPIO26
#     scl: GPIO27
#   data_pins: [GPIO5, GPIO18, GPIO19, GPIO21, GPIO36, GPIO39, GPIO34, GPIO35]
#   vsync_pin: GPIO25
#   href_pin: GPIO23
#   pixel_clock_pin: GPIO22
#   power_down_pin: GPIO32
#   vertical_flip: true
#   horizontal_mirror: false
#   # resolution: 640x480


# Web server to view the camera stream
web_server:
  port: 80

# # Optional: Enable streaming over RTSP
# stream:
#   port: 8554